#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

void PrintMainMenu();

int IntegerInputRange(char* message,int from ,int to);

int ValidatePositiveInteger(char* number);

void IngresarCadena(char* message, char* string);

void InputString(char* message, char* string);
